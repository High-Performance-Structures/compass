# Capture fragments

Only reviewed `*.capture.json` files named by the next-batch manifest belong in
this directory. The validator ignores this README and rejects fragments for
pilot, archived, or unknown templates.
