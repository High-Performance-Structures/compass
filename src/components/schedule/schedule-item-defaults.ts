/**
 * A blank schedule item should not silently represent a full workweek.
 * Template and import paths provide their own explicit workday counts.
 */
export const DEFAULT_NEW_SCHEDULE_ITEM_WORKDAYS = 1
